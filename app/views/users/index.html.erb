<h1>Lodge Members</h1>

<p id="notice"><%= notice %></p>

<table>
    <thead>
        <tr>
            <th>Email</th>
            <th>Activated</th>
            <th>Admin</th>
            <th></th>
            <th></th>
            <th></th>
        </tr>
    </thead>
    <tfoot>
        <tr>
            <td></td>
            <td></td>
            <td></td>
            <td></td>
            <td></td>
            <td></td>
        </tr>
    </tfoot>
    <tbody>
    <% @users.each do |user| %>
        <tr>
            <td><%= user.email %></td>
            <td><%= user.active %></td>
            <td><%= user.admin? %></td>
            <td class="control"><%= link_to 'Show', user %></td>
            <td class="control"><%= link_to 'Edit', edit_user_path(user) %></td>
            <td class="control"><%= link_to 'Destroy', user, :confirm => 'Are you sure?', :method => :delete %></td>
        </tr>
    <% end %>
    </tbody>
</table>

<br />

<%= link_to 'Invite a new user', new_user_path %>

